pipeline {
  agent any
  stages {
    stage('Checkout Source Code') {
      steps {
        echo 'Checkout source code from SCM'
      }
    }

    stage('Docker build') {
      steps {
        echo 'Build Docker Image'
      }
    }

  }
}